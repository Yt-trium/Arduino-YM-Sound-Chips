/* The idea is the following : The YM2203 is a YM2149 + FM
 * The YM2203 is very cheap (even cheaper than YM2149)
 * This project will not use the OP-O serial outputs (which use an YM3014 as DAC)
 * but only the three analog channels.
 */

// The project is created and tested with an arduino nano,
// but it should be compatible with other types of arduino.

// Define IO pinout

void setup() {

}

void loop() {

}
